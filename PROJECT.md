# CURRENT STATE — September 25, 2026: one checkout, one branch (reconciliation, Claude Code)

Production is https://mj2.pro/, built only from `main` and served by GitHub Pages `gh-pages` `94016fa` (source `b838034`; release record below). The reconciliation deployed nothing. The five content-studio posts are live with their living infographics (`/blog/search-results-by-intent/`, `/blog/optimizing-for-ads-in-free-llm-answers/`, `/blog/growth-title-market/`, `/blog/b2b-vs-b2c-by-vertical/`, `/blog/statistician-vs-data-scientist/`), next to four earlier posts and 25 study notes; the sitemap lists 73 URLs.

| Item | Value |
|---|---|
| Canonical checkout | `/Users/mitchellmiler/Documents/mitchjmiller-html-migration`, on `main` since 2026-09-25 (it was on `claude/agency-redesign`). It is the only working checkout of this repository (see the second checkout below). |
| Branch rule | All work lands on `main` through short-lived branches fast-forwarded in. No force pushes, no second checkout and no long-lived worktree (see AGENTS.md). |
| Archived / kept | Branch `claude/agency-redesign`: tag `archive/agency-redesign-2026-09-25` at its tip `4ec97df`; the branch stays on `origin` (not deleted). Second checkout `~/Documents/mitchjmiller.com`: **left in place, not archived**, because Mitch, 2026-09-25: "we can keep mitchjmiller.com as my personal work history portfolio", separate from M² ([content-lane handoff](handoffs/checkout-reconciliation-2026-09-25.md)). It holds nothing that is not on `origin`: its `main` `f3dc1c6` is an ancestor of `origin/main`, and its only local-only commit, a superseded September 18 legacy `gh-pages` build (`7f7618e`, never deployed), is tag `archive/checkout-b-gh-pages-2026-09-18`. Do not work on this repository there; whether the portfolio gets its own repository is Mitch's decision. |
| Ported to `main` | From the agency branch with `cherry-pick -x`: Stripe for the `mitchjmiller-api` Worker (`114ceec`, code only, **not deployed**), the Tailwind source-scan fix (`fb92c20`), the content-studio records, design-system mirror and QA refresh (`4a97850`, including `content-studio/AGENTS.md` → `OPERATING.md` under the one-record-set rule) and the DNS/HTTPS cutover ledger (`9921673`, `8481ce6`, `690ea66`, `2a54de6`). The agency branch's PROJECT.md and handoff text is appended below as dated history. Its site-standards commit `4ec97df` was already on `main`. The canonical checkout's uncommitted content-studio work (drafts, research, `viz/directions`, `viz/final`, routine runs, OPERATING.md) was committed last. |
| Verification | `npm run typecheck` PASS. `npm run build:release-candidate` PASS: 73 published routes and sitemap URLs, parity 57/57 + 20/20, agency and standards (84/84) unchanged. The release output is byte-identical to a rebuild of `6ad50ce` except for one dead text-transform utility rule that no page uses: the ported scan fix stops emitting it, which renames the one stylesheet (`AgencyLayout.*.css`) that the 77 shell documents reference. Worker: `test-stripe.mjs` 12/12, `test-limits.mjs` 9/9. |
| Gated, not done | Stripe Worker deploy. It needs Mitch's restricted key and webhook secret as Worker secrets, `STRIPE_PRICES`, the `schema.sql` D1 migration and a Stripe webhook endpoint ([runbook](docs/stripe/README.md)). The live Worker stays at the pre-Stripe version `cdf6a033`. Caution: `.github/workflows/cloudflare-worker.yml` deploys the Worker on any push to `main` that touches `cloudflare/api-worker/**` once the repository secrets `CLOUDFLARE_API_TOKEN` and `CLOUDFLARE_ACCOUNT_ID` exist. There were 0 repository secrets on 2026-09-25, so its gate skips the deploy. |
| Record | [handoffs/2026-09-25-one-checkout-reconciliation.md](handoffs/2026-09-25-one-checkout-reconciliation.md). `main` `59e4c31`: the checkout switched to it at 04:47 EDT with `git status` clean. An insurance copy of its 119 pre-switch uncommitted files is at `~/Archive/2026-09-25/mitchjmiller-html-migration-pre-main-switch/`. |

# PUBLISHED — September 25, 2026 (00:11 EDT): posts wave 2 — four posts with living infographics (release owner: Claude Code)

Four posts approved by Mitch on 2026-09-24 ("publish now"), each with one living infographic as its hero: `/blog/optimizing-for-ads-in-free-llm-answers/` (+ `/viz/llm-ads-mandala/`, its own 1200×630 share card), `/blog/growth-title-market/` (+ `/viz/growth-title-market/`), `/blog/b2b-vs-b2c-by-vertical/` (+ `/viz/b2b-vs-b2c-by-vertical/`), `/blog/statistician-vs-data-scientist/` (+ `/viz/statistician-vs-data-scientist/`). All dated September 24, 2026, narrated (Samantha: 16, 10, 17, 7 min). Detail, publication edits and open items: [handoffs/2026-09-24-posts-wave-2.md](handoffs/2026-09-24-posts-wave-2.md). LinkedIn/X not posted (routines own that).

| Item | Value |
|---|---|
| Source | `b838034` on `claude/posts-wave-2`, branched from `main` `489141f`; `main` had not moved at deploy time (fetched 00:06 EDT), so no rebase; `main` fast-forwarded `489141f..b838034` (no force). Built in the temporary clone `/private/tmp/claude-501/mj2-wave2` (removed after the release); the canonical checkout was only read, except the four drafts' `approved: yes` / `approved_by` lines. Drafts and `content-studio/viz/final/` sources re-checked unchanged since the build before the deploy. |
| Deploy | gh-pages `94016fa` (previous `3559667`), pushed 00:10:57 EDT; Pages build `built` 00:11:23 EDT; `cname` mj2.pro, HTTPS enforced. Diff: 109 added (4 posts, 4 narrations, 7 generated cards, 4 embeds with 3 posters + GIF each, the post-2 card, 73 `_astro` bundles), 74 modified (bundle names; `/`, `/blog/`, `/es/blog/` and the sitemap also list the posts), 73 deleted (superseded `_astro` bundles only); `CNAME`, `robots.txt`, `.nojekyll` and the IndexNow key file byte-identical. |
| Artifact | sha256 `574ce45b0cd187600805febd4a85ff1ef0823f5bddd746dc72b999d717a307d9` (523 files, [artifact-manifest.txt](docs/release-2026-09-24-wave2/artifact-manifest.txt)); three builds of `b838034` byte-identical (before and after the commit, and from the pushed `main`); deploy tree hash-matched. |
| QA (release build) | typecheck PASS; `build:release-candidate` PASS: parity 57/57 + 20/20 added, 25/25 bodies, 73 sitemap URLs; agency PASS (73 routes, 6 embeds / 25 files, 17 post FAQ answers mirrored, every embed's declared jump anchors present once in its post); standards PASS (84/84 documents with the full tag set and a 1200×630 card, 0 headshot, narration 9 en + 1 es). Crawl 1063/1063 over 77 routes; browser 537/537 (per new post at 1360/390: fitted same-origin frame, a real click on a sector/title/vertical/stop lands on its anchor, every jump target is a post heading equal to the manifest anchors, listener ignores the page/malformed/non-heading, full-screen embed ignores an off-site `?parent=`, JSON-LD, GIF/posters/card/narration 200); standards suite 793/793 over 84 documents, axe serious+critical 0 in light and dark (the 8 new documents: no axe findings of any impact). Evidence: [docs/release-2026-09-24-wave2/](docs/release-2026-09-24-wave2/). Fix rounds: 4 (narration; bare source URLs overflowing at 390 px → `.prose a` wraps; QA stop click → the stop's own shape; roles skip control covered "Sources" → over the map corner). |
| Live probes 00:12–00:22 EDT | [live-probes.json](docs/release-2026-09-24-wave2/qa/live-probes.json) 91/91: 4 posts 200, one h1, self canonical, `index, follow`, GA4 once, Article (datePublished 2026-09-24) + BreadcrumbList + FAQPage (4/3/3/3), audio element with "Listen to this article (N min)", narration 200 `audio/mp4`, og:image = twitter:image with alt, card 200 PNG 1200×630, one titled iframe with the canonical `?parent=`, noscript poster, full-screen link, every jump anchor once; 4 embeds 200 `noindex, follow`, no analytics, 17 poster/GIF/card files 200; sitemap 73 incl. the 4 posts; robots unchanged; 17 older routes 200 (incl. `/blog/search-results-by-intent/`); `/blog/` links the 4 posts; unknown route 404; `www` 301 → apex. `scripts/qa/live-standards.mjs` 321/321: full share tags 79/79, headshot share images 0, narration 10/10, hreflang 73, reciprocal 73. Live headless, analytics blocked ([live-browser-posts.json](docs/release-2026-09-24-wave2/qa/live-browser-posts.json)) 40/40: each post at 1360/390 loads its embed in a fitted same-origin frame, a real click lands on `#assistant-google-ai-mode`, `#title-director-of-seo`, `#vertical-saas-software`, `#role-pay` at the header line, audio present, 0 console errors; live standards suite on the 8 new documents 85/85, axe 0 serious/critical in light and dark. |
| IndexNow | 7 URLs (the 4 posts, `/blog/`, `/es/blog/`, `/`), host mj2.pro, existing key file (200): HTTP 200, 00:22:48 EDT. |
| Rollback | in a clean gh-pages worktree: `git revert --no-edit 94016fa && git push origin gh-pages` (restores deploy 2's `3559667` tree). Source: `git revert b838034` on `main` (the record commit after it is documentation only). |

What changed in source besides the posts: the `viz-intent` listener (`site/components/parity/enhance.ts`) accepts a validated `anchor` from the post's own frame (`h2`–`h4` inside the post body; default `#intent-<intent>` unchanged); posts may bring a ready-made 1200×630 card (`card: { src, alt }` in `src/lib/data.ts`; `site/lib/share.ts`, `AgencyLayout.astro`, `[...path].astro`, `scripts/share-cards.mjs`); `.prose a` wraps; the route manifest declares each embed's jump `anchors` (read by `verify-agency.mjs` and `scripts/qa/browser.mjs`); route count 69 → 73.

Open for Mitch: (1) post 2's first-person line ("The prompt-set tracking I've built…") paraphrases the AEO case study; (2) post 3's ZipRecruiter $101,752 and Glassdoor $359,824 could not be re-fetched automatically (403); a signed-in check is worth it; (3) the live "Search Results by Intent" post still says the ads follow-up is "— coming"; linking it needs that post's narration re-rendered; (4) no keyword research (Semrush UI) for these posts; (5) Search Console: resubmit `https://mj2.pro/sitemap.xml` (73 URLs). For the content kit owners: `content-studio/viz/` templates still lack the published fixes (group role, skip control, same-origin jump, `anchor` in the message).

# PUBLISHED — September 24, 2026 (21:55 EDT): site standards, deploy 2 — Spanish pilot (release owner: Claude Code)

Deploy 2 of 2 of the site standards ([docs/site-standards.md](docs/site-standards.md), brief [handoffs/2026-09-24-site-standards-build.md](handoffs/2026-09-24-site-standards-build.md)): Spanish versions of the core pages and one post, hreflang on every page, a globe language picker and a browser-language suggestion banner that never redirects. The same release ships the study note `/blog/studying/figma-shortest-course/`, wired per the handoff another session left on `main`.

| Item | Value |
|---|---|
| Source | `c9b3c48` (Spanish pilot) on `main` after deploy 1's records `eb8b9d7`. Before the deploy `main` moved (`c9b3c48..f3dc1c6`, another session: `4dea915` redirect handoff, `1ddc06e` study note, `f3dc1c6` wiring handoff), so the branch was fast-forwarded to it, the note wired (`c04e56e`: manifest route, count 68 → 69) and everything rebuilt and re-verified; `main` fast-forwarded `f3dc1c6..c04e56e` (no force). Built in the temporary clone; the canonical checkout was only read. |
| Deploy | gh-pages `3559667` (previous `6edd43f`), pushed 21:55:06 EDT; Pages build `built` 21:55:30 EDT; `cname` mj2.pro, HTTPS enforced. Diff: 86 added (5 Spanish pages, the note, 6 cards, 1 narration, 73 `_astro` bundles), 69 modified, 73 deleted (superseded `_astro` bundles only); `CNAME` and `robots.txt` byte-identical. |
| Artifact | sha256 `4b76d8ad7f1926839c32f3451a8651dd071bfd1b65a14fa4aed43a7b9cb603bd` (487 files, [artifact-manifest.txt](docs/release-2026-09-24-spanish/artifact-manifest.txt)); two clean builds of `c04e56e` byte-identical; deploy tree hash-matched. |
| QA (release build) | typecheck PASS; `build:release-candidate` PASS: parity 57/57 + 16/16 added, 25/25 bodies, 69 sitemap URLs; agency PASS (69 routes, 4 marquees incl. /es/, 12 FAQ questions mirrored incl. /es/services/); standards PASS (76/76 documents with the full tag set and their own card, 0 headshot references, narration 5 en + 1 es current, hreflang on 68 en + 5 es pages, 69/69 sitemap entries equal to the page tags, 72 pickers). Crawl 986/986 over 73 routes; browser 389/389 (incl. the five Spanish pages at 1360/390); standards suite 709/709 over 76 documents: axe 0 violations of any impact in light and dark, banner (Spanish browser → Spanish suggestion, English browser or English ranked first → none, English browser on /es/ → English suggestion, no translation → no banner, dismissal remembered, never a redirect), picker (Enter/Escape/focus return, choice remembered, untranslated page → Spanish home, labelled), Spanish contact messages against a mocked Worker, desktop header fits at 1281/1360/1500 in both languages. Evidence: [docs/release-2026-09-24-spanish/](docs/release-2026-09-24-spanish/). |
| Live probes 21:56–22:33 EDT | 5 Spanish routes 200, `lang="es"`, one h1, self canonical, `index, follow`, hreflang en/es/x-default; their English pages link back; untranslated pages carry self + x-default; the note 200; sitemap 69 URLs with 148 alternates; robots unchanged; `/es/<unknown>` 404; `www.mj2.pro/es/` 301 → apex; Spanish narration 200 `audio/mp4`. `scripts/qa/live-standards.mjs`: 289/289, **full share tags 71/71, headshot share images 0, narration 6/6, hreflang on 69/69 sitemap pages, reciprocal 69/69**. Live headless standards suite on `/services/`, `/es/services/`, `/es/contact/` and the Spanish post ([live-browser-standards.json](docs/release-2026-09-24-spanish/qa/live-browser-standards.json)): 71/71: axe clean in light and dark, theme set before first paint, the banner in all five preference cases with no redirect and a remembered dismissal, the picker, Spanish form messages, cards 200, the Spanish narration plays. Two earlier full-suite live runs stopped on a stalled CDN navigation (60 s timeout on a page that loads in under 1 s on its own); the suite now retries a timed-out navigation once on deployed origins and records a page that still fails instead of stopping. |
| IndexNow | 69 sitemap URLs (5 new Spanish, 1 new note, every other head changed): HTTP 200, 22:01:53 EDT. |
| Rollback | in a clean gh-pages worktree: `git revert --no-edit 3559667 && git push origin gh-pages` (restores deploy 1, `6edd43f`). Source: `git revert c04e56e c9b3c48` on `main`; `main` then builds only if the note source `1ddc06e` (another session's) is reverted too or re-wired. |

What shipped:
- **Spanish routes** (manifest kind `added`, `lang: "es"`, `translationOf`): `/es/` (home), `/es/services/` (the brief allowed "about or services"; services is the commercial page for the local-business audience), `/es/contact/` (same Worker form contract and topic values as `/contact/`, Spanish labels, Turnstile `data-language="es"`, Spanish status messages), `/es/blog/` (the translated post, then the English posts marked "en inglés"), `/es/blog/gbp-2026-ai-grounding/` (translation in `site/i18n/es-posts.ts`, inline SVG infographic translated and refitted, Article JSON-LD `inLanguage: es` with `translationOfWork`, narration by **Paulina** (es_MX), 9 min, 3.4 MB). Links from Spanish pages to English-only pages carry `hreflang="en"` and are marked in the copy where a reader would otherwise be surprised. Legal/privacy pages: none exist on the site.
- **Translation process**: model draft (neutral Latin American Spanish, "tú"; SEO, AEO/GEO, CTR, ROI, NAP, Local Pack, AI Overviews, Google Business Profile and product names kept in English), then an **independent review pass in a separate model context**: approve after fixes; 32 findings (1 must-fix: "La política más estricta de Google" read as a superlative, where the English says the policy was *tightened*; 21 should-fix; 10 optional), all applied; numbers, dates and sources checked equal to the English; Google's Latin American UI terms confirmed ("Perfil de Negocio de Google", "Rendimiento", "Novedades", "popularidad", "Instrucciones sobre cómo llegar"). Each Spanish route's manifest record says `reviewed: true`, native-speaker check pending.
- **Every page**: `<html lang>`; hreflang self + translation + `x-default` → English, generated from one map (`site/lib/i18n.ts`, derived from the manifest) and never to a page that does not exist; `og:locale` `en_US`/`es_MX` with `og:locale:alternate` where a translation exists; sitemap `xhtml:link` alternates equal to the page tags (the SFC report gets its tags in finalize). Header: globe language picker (a `<details>` button labelled "Language: English" / "Idioma: Español"; its menu lists "English" and "Español" in their own script, no flags; Escape and outside click close it; the choice is remembered in `mj2-lang`); on /es/ pages the two English-only utility links leave the desktop header (they stay in the footer and mobile menu) so the full navigation fits. **Suggestion banner**: rendered hidden where a translation exists; shown only when the browser's languages rank the translation's language above the page's own (or it was chosen before); "No, gracias" / "No, thanks" is remembered; the script never changes location (checked at build).
- **Checks changed on purpose**: verify-agency and the crawl check the shell with Spanish labels on /es/ routes (same checks, per language) and count 69 routes; TODO/TBD markers are matched case-sensitively (the Spanish word "todo" is content); verify-standards gained the language checks; the standards suite gained banner, picker, Spanish-form and header-fit tests.

Open for Mitch (in addition to deploy 1's list): (1) a native-speaker spot check of the five Spanish pages (the review record names the terms and choices to look at: "tú" register, kept-English terms, "grounding"); (2) `og:locale` is `es_MX` (a valid ISO pair Facebook accepts; the text itself is neutral Latin American Spanish): keep, or choose another; (3) after 60–90 days, Search Console impressions and clicks by language and country decide whether more pages or languages follow (standards).

# PUBLISHED — September 24, 2026 (20:19 EDT): site standards, deploy 1 (release owner: Claude Code)

Mitch's 2026-09-24 standing instruction ([docs/site-standards.md](docs/site-standards.md), build brief [handoffs/2026-09-24-site-standards-build.md](handoffs/2026-09-24-site-standards-build.md)), deploy 1 of 2: share cards on every route, light/dark themes, WCAG 2.2 AA baseline, narrated posts. Deploy 2 (Spanish pilot) follows in a separate release.

| Item | Value |
|---|---|
| Source | branch `claude/site-standards` from `origin/main` `b4d67b6`: `b79832d` (standards, brief, AGENTS.md pointer) + `3d2a026` (implementation); `main` fast-forwarded `b4d67b6..3d2a026` (no force). Built in the temporary clone `/private/tmp/claude-501/mjm-site-standards`; the canonical checkout (on `claude/agency-redesign`, other workers' uncommitted changes) was only read. |
| Deploy | gh-pages `6edd43f` (previous `7844899`), pushed 20:19:51 EDT; Pages build `built` 20:20:20 EDT; `cname` mj2.pro, HTTPS enforced. Diff: 148 added (70 cards under `/og/`, 5 narrations under `/audio/blog/`, 73 `_astro` bundles), 70 modified, 73 deleted (all superseded `_astro` bundles); `CNAME` (`mj2.pro`, no newline) and `robots.txt` byte-identical. |
| Artifact | sha256 `fcb3f68b675e5c8afc5abd9bc48ad798cee75c695286f892e7cde3bc76c22570` (474 files, [artifact-manifest.txt](docs/release-2026-09-24-standards/artifact-manifest.txt)); two clean builds of `3d2a026` byte-identical (cards included); deploy tree hash-matched. The hash only reproduces from the same build directory path (Astro hashes absolute paths into island `uid`s; the live 7844899 tree differs from a rebuild elsewhere only there). |
| QA (release build) | typecheck PASS; `build:release-candidate` PASS: parity 57/57 + 10/10 added, 25/25 bodies, agency PASS, new verify-standards PASS (70/70 documents with the full tag set and a local 1200×630 card, 0 headshot references, dark-token blocks identical, 40 dark text pairs ≥ 4.5:1, 5/5 posts narrated and current). Crawl 908/908; browser 319/319; new standards suite 620/620 over 70 documents: axe-core 4.13.0 WCAG 2.0–2.2 A/AA, **0 serious or critical in light and in dark**, theme set before `<body>` in both system schemes, toggle/persistence/keyboard/blocked storage/JS-off, 0 overflow at 390 px on every document, 70 cards 200 image/png 1200×630, 5 narrations 200 audio/mp4 and playing. Evidence: [docs/release-2026-09-24-standards/](docs/release-2026-09-24-standards/). Fix rounds: 5 (harness observer bug; stale 404 cache in `serve.mjs`; pre-existing contrast failures; stylesheet-order tie on the workbench). |
| Live probes 20:20–20:23 EDT | 9 spot routes 200, one h1 (embed none), canonical and robots as before, pre-paint theme script on shell pages and the SFC report; sitemap 63; robots unchanged; unknown route 404; `www` 301 → apex. `scripts/qa/live-standards.mjs` re-crawl of the live sitemap + 2 embeds: 201/201, **full share tags 65/65, headshot share images 0 (was 39), narration 5/5**. Live headless spot checks (post, services): 38/38 incl. axe both themes, no flash, persistence, audio plays. |
| IndexNow | 63 sitemap URLs (every page's head changed), host mj2.pro: HTTP 200, 20:22:33 EDT. |
| Rollback | in a clean gh-pages worktree: `git revert --no-edit 6edd43f && git push origin gh-pages` (restores the `7844899` tree). Source: `git revert 3d2a026` on `main` (`b79832d` is documentation only). |

What shipped:
- **Share cards** (`scripts/share-cards.mjs`, template `scripts/share-card.html`, logic `site/lib/share.ts`): at build time every document gets `/og/<route>.png` (1200×630, section + page title + mj2.pro, navy/green, no photo, no logo), including the 4 Coming Soon placeholders, 404, the standalone SFC report and the `/viz/` embeds (still noindex, no GA4). A post's card shows its own living-artifact poster when it declares an embed (`embeds[].embeddedIn`, `poster-1080x1350.png`) or a `hero` path in its data; otherwise the text card. Full tag set: `og:title/description/url/type/locale/site_name/image(+width,height,type,alt)` and `twitter:card=summary_large_image/title/description/image/image:alt`; `twitter:site` waits for Mitch's handle. The layout's transient `mj2:card` marker is removed from the output. No project image met 1200×630, so none is used; third-party screenshots are never composed into cards. `parityMetadata` no longer falls back to the headshot; CreativeWork JSON-LD uses the project image or the card; Organization `image` is the home card (`logo` unchanged, see below).
- **Themes** (`site/styles/standards.css`, `site/lib/theme-script.mjs`): system default; pre-paint inline script (stored choice via try/catch, else system) so no flash; header button with `aria-pressed` ("Dark theme"), 44×44, visible at every width, keyboard operable, remembered in `localStorage` `mj2-theme`; follows live system changes only without a stored choice; JS-off visitors get the system theme and no button. Light mode is pixel-identical on home/services/products below the header; elsewhere only the accessibility fixes changed. Dark values are one token set declared twice (system preference and explicit choice), checked identical at build. Light islands in both themes: logo marquee, Mermaid diagrams, white/slate image frames, lab experiments, population workbench. The SFC report keeps its own palette and now follows the stored site choice.
- **Accessibility** (axe, both themes, every document): muted chips (4.34 → 5.07:1 via `--muted-foreground`), green on green tints (4.35 → ≥5.6), lab step numbers (3.2), workbench bar counts (3.9), study-note Buzz text (4.25), a navy-on-navy heading on /collab-ideas/ and /aeo-geo/ (1.0), faint AEO/GEO step numbers (1.8), the SFC report's `--mute/--kelp/--coral` and dark pills (head CSS only, body byte-identical), study-note source links underlined (1.4.1), diagrams `role="figure"`, the mandala SVG `role="group"` (its sector buttons were hidden inside `role="img"`), wide post tables wrapping at 390 px, and a "Skip animation" button for the two infographic intros (8.5 s and 11 s; WCAG 2.2.2). Workbench headings are pinned to its own rules (a stylesheet-order tie flipped when the shell CSS changed).
- **Narration** (`scripts/narrate.mjs`, `site/data/narration.json`, `public/audio/blog/<slug>.m4a`): all 5 published posts from the merged list (`baseline/src/lib/published.ts`, so posts in `src/lib/` are included): title + body prose, code blocks skipped, each table summarized in one sentence; macOS `say` with the best installed voice (today compact **Samantha**, en_US), mono AAC 48 kbps CBR via `afconvert`; 55 min, 20 MB. "Listen to this article (N min)" `<audio controls preload="none">` under the teaser, with a note that it is computer-generated and the article text is the transcript.

**Narration command for new posts** (the build fails without it): `npm run narrate -- <slug>` (English), `npm run narrate -- --lang es <slug>` (a translation), `npm run narrate -- --missing` (every post without current audio), `npm run narrate -- --all` (re-render everything, e.g. after installing a Premium voice); then commit `public/audio/…/<slug>.m4a` and `site/data/narration.json`. Cards need no command. Posts are blog posts; the 24 study notes are not narrated (see below).

Open for Mitch: (1) final M² logo file: then replace the selfie header logo, `public/favicon.{ico,png,svg}`, `public/apple-touch-icon.png` and the Organization JSON-LD `logo` (still the retired 2026 social card); (2) his X handle for `twitter:site`; (3) optional Premium voice (System Settings → Accessibility → Spoken Content → Manage Voices), then `npm run narrate -- --all`; (4) whether the 24 study notes (`/blog/studying/`) count as posts for narration: measured 22,474 narration words, about 155 min at the measured 145 wpm (6.5 min each), about 57 MB at 48 kbps; (5) the redesign branch `claude/agency-redesign` must adopt these standards (layout, `site/styles/standards.css`, the scripts and checks) before it is ever released. For the content kit owners: the mandala and journey templates under `content-studio/viz/` still carry `role="img"` around buttons and no stop control for their >5 s intros; the published copies are fixed, the kit is not (not touched: other workers' files).

# PUBLISHED — September 24, 2026: "Search Results by Intent" on mj2.pro (release owner: Claude Code)

Mitch, 2026-09-24: "publish now" (site only; LinkedIn not authorized and not posted). Live: https://mj2.pro/blog/search-results-by-intent/ with embeds https://mj2.pro/viz/search-results-by-intent-mandala/ and https://mj2.pro/viz/search-results-by-intent-journey/. Same release restores the three study notes the 2026-09-21 build omitted: `/blog/studying/pocock-ai-coding-workflow/`, `/fde-1m-ai-job/`, `/pocock-agentic-workflow/` ([gap handoff](handoffs/study-notes-deploy-gap-2026-09-22.md) resolved).

| Item | Value |
|---|---|
| Production provenance | gh-pages serves `main`: release `0b9073d` (2026-09-21) was built from `main` `bb32728` (recorded on `claude/agency-redesign` `1c89a2c`/`122630c`); Pages custom domain `mj2.pro` since gh-pages `3cdf29f` (2026-09-23 Pages settings re-add for certificate issuance). The 2026-09-22 "do not publish while paused" hold is treated as lifted: `3cdf29f` created the `mj2.pro` CNAME, `https://mj2.pro/` served 200 before this release, and Mitch said "publish now". |
| Source | branch `claude/post-search-results-by-intent` `4fcfd6f` from `origin/main` `fcb16d0`; `main` fast-forwarded `fcb16d0..4fcfd6f` (no force). Built in a temporary clone because the canonical checkout is on `claude/agency-redesign` with other workers' uncommitted changes. |
| Deploy | gh-pages `7844899` (previous `3cdf29f`), pushed 18:20:10 EDT; Pages build `built` 18:20:32 EDT; `cname` mj2.pro, HTTPS enforced. Diff: 26 files added, HTML pages changed only in bundle references, 7 superseded `_astro` bundles removed; `CNAME` and `robots.txt` byte-identical. |
| Artifact | sha256 `5d5aaa5a53785389b476d3ab97594c58123528c8a85d8d8d670f3434ebddd1b7` (sorted manifest [docs/release-2026-09-24/artifact-manifest.txt](docs/release-2026-09-24/artifact-manifest.txt), 399 files); rebuilt from `4fcfd6f` and hash-matched, deploy tree hash-matched. |
| QA | typecheck PASS; `build:release-candidate` PASS (parity 57/57 + 10/10 added, 25/25 bodies, 63 sitemap URLs; agency PASS incl. new post/embed checks); crawl 908/908 over 67 routes; browser 319/319 (both frames load and fit, real sector click → panel → `#intent-local`, journey click → `#intent-transactional`, listener ignores foreign senders, off-site `?parent=` ignored, JSON-LD parses, GIF/posters served, no overflow at 390, no console errors). Evidence: [docs/release-2026-09-24/](docs/release-2026-09-24/). Fix rounds: 2 (mandala lacked `</head>` for the robots tag; "see draft notes" leaked into the body). |
| Live probes 18:21–18:23 EDT | post 200, one h1, canonical `https://mj2.pro/blog/search-results-by-intent/`, `index, follow`, GA4 once, JSON-LD Article (datePublished 2026-09-24) + BreadcrumbList + FAQPage (4), 2 iframes, 5 `#intent-*` anchors; both embeds 200 `noindex, follow`; 2 GIFs + 6 posters + 5 renderings 200; 3 restored notes 200; sitemap 63 incl. the post; robots unchanged; 15 prior routes 200; unknown route 404; `www.mj2.pro` 301 → apex; `http://mitchjmiller.com/<path>` 301 → mj2.pro → 200. Live headless check at 1360/390: frames fitted, territory click lands on `#intent-local`, 0 console errors. |
| IndexNow | 6 URLs (post, 3 notes, `/blog/`, `/`), host mj2.pro, existing key file: `202 Accepted` 18:23:37 EDT (key validation pending for the new host). |
| Rollback | in a clean gh-pages worktree: `git revert --no-edit 7844899 && git push origin gh-pages` (restores the `3cdf29f` tree). Source: `git revert 4fcfd6f` on `main`. |

How it is built: posts/notes published after snapshot `2745c7e` live only in `src/lib/`; `baseline/src/lib/published.ts` prepends them (never replacing an archived slug), so the archive and the 57-route parity reference stay untouched. Each must be a manifest route of kind `added`; `/viz/*` standalone embeds are declared under `"embeds"` in `docs/implementation-2026-09-11/route-manifest.json` (noindex, outside the shell, no analytics: the post is the indexable page). `verify-agency.mjs` now fails on undeclared `/blog/` or `/viz/` documents.

Publication edits versus the approved draft (draft file itself only got the approval lines and mj2.pro URLs): body `# title` dropped (template renders the h1); internal `<!--EMBED-->` notes and "see draft notes" removed; FAQ questions as h3 with FAQPage JSON-LD mirroring the visible answers word for word; link to the unpublished follow-up post de-linked; SERP captions "Representative rendering; real capture pending." without the session/IP note, alt text describes each rendering; research-folder sentence dropped; the draft's `meta_description` used as the meta description; **Ahrefs featured-snippet citation corrected** to its verified primary source (Ahrefs, Ryan Law, 22 July 2025, 1M US desktop SERPs, 15.41% → 5.53%; the draft's URL was the 2017 study). Published viz copies: same-origin jump fallback (embedding post, then same-origin `?parent=`, else the post), mandala reads `/images/blog/serp-examples/` with honest captions and signs `mj2.pro` (posters/GIF re-rendered, 0 text under 14 px, 0 overlaps), journey live mode scales to its frame. `enhance.ts`: same-origin `viz-intent` listener plus frame auto-height. `CNAME` now emitted byte-identical to Pages (`mj2.pro`, no newline).

Open for Mitch: (1) `https://mitchjmiller.com` still times out (Namecheap forwarding has no TLS; HTTP forward works); options and the five email-forwarding MX records to preserve are in the site-migration record on `claude/agency-redesign` (`122630c`). Nothing touched. (2) Real SERP captures need a US IP; when they land, replace `public/images/blog/serp-examples/<intent>-desktop.png` and update the captions/alt in `src/lib/data.ts` and the mandala copy's rendering caption. (3) Post og:image is the site-default headshot (site convention); a per-post card would improve link previews. (4) LinkedIn not posted; the draft now links to mj2.pro; the blog-syndicate gate wants a `link:` header with UTM; content-studio posters still sign "mitchjmiller.com", so use the published copies in `public/viz/search-results-by-intent-mandala/`. (5) Search Console: submit `https://mj2.pro/sitemap.xml` (63 URLs). (6) Records diverge: `main` lacks the Sept 22–23 release/DNS/Stripe notes that live only on `claude/agency-redesign`; reconcile the branches once the other workers commit. *(2026-09-25: item 6 is resolved by the reconciliation at the top, and the site-migration record cited in item 1 is now on `main`: [docs/site-migration-2026-09-21/README.md](docs/site-migration-2026-09-21/README.md).)*

# Current migration — September 21, 2026

Mitchell approved the `mj2.pro` domain cutover, the agency redesign, the approved public/private site tree and the M² business identity. It was executed in the canonical checkout (then on `claude/agency-redesign`, on `main` since the September 25 reconciliation at the top) and shipped on 2026-09-21 as gh-pages `0b9073d` from `main` `bb32728`; `mj2.pro` DNS and HTTPS went live on 2026-09-23. The release will keep GA4 `G-HCKYWCZQ8E` for continuity, keep staging analytics-free, and keep the private portal analytics-free. See [site migration record](docs/site-migration-2026-09-21/README.md) for the approved tree, templates, redirect plan, tradeoffs and progress ledger.

Gate at the time: build and validate the new-domain release, publish it, activate the client portal hostname, then complete GSC/GA4/LinkedIn updates. Status 2026-09-25: release, DNS and HTTPS are done; the ledger's other items (HTTPS for the legacy `mitchjmiller.com` redirect, `clients.mj2.pro`, GSC/GA4 verification, the LinkedIn/M² page, Rio's invite) are not recorded as done anywhere. The M² LinkedIn page will use the new squared identity; Rio's invite requires exact profile resolution before it is sent.

# History from `claude/agency-redesign` — September 22–23, 2026 (ported 2026-09-25)

Verbatim top section of the archived branch's PROJECT.md (`4ec97df`, written by commits `b3a2648`, `64a5a93`, `28a08fd`, `f74b887` and `b824faa`), which `main` lacked. It predates the September 24–25 releases above: where it names `claude/agency-redesign` as the checkout branch or calls the redesign incomplete, the reconciliation section at the top is current. Its intermediate September 22 gate ("the site is dark on both domains", `b3a2648`) was superseded by the September 23 text below and stays in the archived branch.

## Current migration — September 23, 2026 (as recorded on the archived branch)

Mitchell approved the `mj2.pro` domain cutover, the agency redesign, the approved public/private site tree and the M² business identity. The canonical checkout is on `claude/agency-redesign` at `f74b887`, pushed to GitHub. GitHub Pages serves the release from `gh-pages` at `0b9073d`; its custom domain is `mj2.pro`. See [site migration record](docs/site-migration-2026-09-21/README.md) for the approved tree, templates, redirect plan, tradeoffs and progress ledger.

Current gate: **legacy-domain HTTPS forwarding and measurement.** On September 23, 2026, DNS was verified against GitHub Pages, the old domain's HTTP 301 was verified to preserve path and query, and HTTPS was enabled after GitHub approved a certificate for `mj2.pro` and `www.mj2.pro` through 2026-12-22. The latest Pages build completed successfully. Live checks: `https://mj2.pro/` 200; `https://www.mj2.pro/` redirects to the canonical host and returns 200; old-domain HTTP path/query redirects correctly. `https://mitchjmiller.com/...` still times out at the Namecheap forwarding endpoint. The old domain currently has five Namecheap email-forwarding MX records; preserve these and other DNS records if moving DNS to an HTTPS-capable redirect provider. See [site migration record](docs/site-migration-2026-09-21/README.md) for the next steps. The source and live `gh-pages` HTML contain the GA4 base loader for `G-HCKYWCZQ8E`; no verified custom CTA, form, download or tool-interaction events are implemented. Next, resolve secure forwarding for the old domain, then verify the GSC sitemap and GA4 realtime. The M² LinkedIn page will use the new squared identity; Rio's invite requires exact profile resolution before it is sent.

The delegated continuation task ended idle without a completion report, so the redesign is not being marked complete. The durable continuation record is [the M² redesign and analytics handoff](../Codex/2026-07-29/mitch-command-center/handoffs/2026-09-22-m2-redesign-analytics-handoff.md).

September 22 contact-Worker fix: the deployed `mitchjmiller-api` Worker still carried its September 11 origin allowlist and rejected `https://mj2.pro`, which would have broken every contact submission after cutover. Redeployed from committed source (version `cdf6a033`, rollback `8a08874e`) and verified by CORS probe. Also scoped Tailwind's auto-detected sources so the newly committed `src/ds` mirror and content-studio prose stop emitting unused utilities; re-verified with crawl 844/844 over 63 routes and browser QA 233/233.

September 22 housekeeping: the previously uncommitted content-studio work, `.design-sync` previews, the `src/ds` React component mirror and refreshed QA screenshots are committed and pushed on `claude/agency-redesign` at `2b73738`. Generated infographic exports (~114 MB) are gitignored and regenerate with `cd content-studio/viz/kit && npm run export`.

September 22 Stripe integration (Payments, Invoicing, Tax) for the M² business: built and locally verified, **not deployed**. The `mitchjmiller-api` Worker gains `POST /checkout` (Stripe-hosted Checkout from a server-side price allowlist, dynamic payment methods, tax-ID collection, invoice creation) and `POST /stripe/webhook` (signature-verified, idempotent by event id, `stripe_events` and `payments` tables in D1, paid only when `payment_status` is not `unpaid`), plus an invoice CLI and a key-leak pre-commit hook. Stripe Tax is gated off (`STRIPE_AUTOMATIC_TAX = "false"`) until a registration is verified with the tax advisor. Evidence: `test-stripe.mjs` 12/12, `test-limits.mjs` 9/9, `wrangler deploy --dry-run` bundles. Blocked on Mitch: Stripe MCP/CLI login, sandbox and restricted keys as Worker secrets, product catalog → `STRIPE_PRICES`, webhook endpoint, tax decision. Records: [Stripe runbook](docs/stripe/README.md), [Stripe handoff](handoffs/2026-09-22-stripe-integration.md).

# Previous hold — September 15, 2026

The latest voice direction pauses the new reference-matching homepage work while Mitchell compares Contiem’s homepage with its Technical Writing page. No changes were started for that new request. Earlier home/Services refinement and the ten-logo/five-palette comparison remain saved locally and validated; they were not committed, pushed or newly deployed by this turn. Do not publish while paused. The existing private host still carries the earlier version 4 recorded below. *(2026-09-25: this hold is lifted. The September 24 release record above treats it as lifted after Mitch's "publish now", and production has shipped from `main` since 2026-09-21.)*

Logo feedback: option 10, **Roundel**, is the closest starting point, but none of the ten is approved. Editorial and Principal Signature were rejected. A separate design agent owned by the originating voice task handles the math/data-inspired next round; this task’s duplicate generation assignment was stopped. No replacement or selected palette is authorized. Resume only after Mitchell settles the reference and asks implementation to continue. The intended main CTA opens the existing contact form.

# Refinement — September 14, 2026 (history: the design shipped with the 2026-09-21 release; the M² logo choice is still open)

The centered home/Services refinement and ten M² / five palette comparison are implemented and validated in the single canonical checkout on `claude/agency-redesign`. One hero CTA, actual brand logos before Strategy through delivery, solid topic icons, shorter copy, native evidence/FAQ disclosures. Logo and palette await Mitchell’s choice. Read [current refinement](docs/redesign-2026-09-14/refinement.md), [copy/positioning](docs/redesign-2026-09-14/concise-copy.md) and [options](docs/redesign-2026-09-14/palette-options.md). Private review publication is the remaining lead step; public production is unchanged by this task. Earlier entries below are historical.

# Earlier refinement checkpoint (superseded below in history)

September 14: agency + software + portfolio refinement implemented on `claude/agency-redesign`; staging build/typecheck and 348 changed-page link checks pass. Private staging publication and read-only Claude review are complete. Review: https://mitch-portfolio-html-staging.clearkayakrentalsoah.chatgpt.site . Direct design review fixes were applied; lead notification and conversion tracking are recorded follow-ups. M² is a proposed brand, not selected or implemented. See [current refinement record](docs/redesign-2026-09-14/README.md). Public production remains unchanged by this task; earlier release records below remain historical evidence.

Content draft (September 14, 19:35 EDT, Claude Code): new Writing post draft `content-studio/drafts/2026-09-14-growth-engineering-without-the-title.blog.md` (untracked, unpublished; studio gate elements present; Semrush volumes unverified, API units exhausted). Publish mechanics unchanged (`blogPosts` entry in `src/lib/data.ts`). Two claims flagged for Mitch in the draft notes. Codex check-in: `/Users/mitchellmiler/Documents/ai-os/ops/overnight-2026-09-11/status-content-blog-2026-09-14.md`. No source, staging or production change. 19:55 EDT correction from Mitch: the narrative draft conflated the brief; the assignment is data-viz-first posts (stat-shock copy anointing vivid, moving, interactive artifacts) plus two scheduled routines (blog research 1–3x/week, LinkedIn syndication). Content-engine agents are now active ONLY under `content-studio/research/`, `content-studio/viz/`, `content-studio/routines/`, `content-studio/drafts/` and a new project folder `/Users/mitchellmiler/Documents/li-syndication/`; they do not touch site/, src/, public/ or root records. Scheduled local tasks created 21:2x EDT: `blog-research-dataviz-draft` (Mon/Wed/Fri 06:00 ET, research + data-viz draft, never publishes) and `linkedin-syndication-queue` (Tue/Thu/Sat 07:00 ET, queue for one-tap approval, never posts); playbooks in `content-studio/routines/` and `/Users/mitchellmiler/Documents/li-syndication/`.

Content engine outcome (23:31 EDT, Claude Code): four living infographics built from the kit at `content-studio/viz/kit/` with real, sourced data: `growth-title-market`, `search-results-by-intent`, `statistician-vs-data-scientist` and `b2b-vs-b2c-by-vertical` (critic C re-check passed after fixes); post drafts and LinkedIn teases in `content-studio/drafts/2026-09-14-<slug>.blog.md` and `.linkedin.md`, all `approved: no`; critic reports in `content-studio/routines/runs/2026-09-14-critic-A.md`, `-B.md`, `-C.md`. Private review: https://claude.ai/artifact/MeCGHxD3j722cYJ371bTTd . Publish path for the release owner after Mitch's approval: copy `content-studio/viz/<slug>/index.html` to `public/viz/<slug>/index.html`, add the `blogPosts` entry with the FAQPage block, then RELEASE-READY.md. Still no site/src/public edits and no commits from the content agents.

Infographic direction reset (2026-09-15 07:59 EDT, Claude Code): Mitch rejected the first visual layer (dark dashboard tiles) as a conflation of the site's austere palette with the infographic brief (bright, mandala/fractal-structured, motion-first, dense reference posters per `content-studio/PIPELINE-2026-09.md`). Five competing directions were built on the search-intent ledger under `content-studio/viz/directions/01-data-mandala`, `02-fractal-stack`, `03-reference-poster`, `04-neon-arcade`, `05-journey-map` (each: self-contained index.html, poster PNGs, looping GIF, README). Pick-one page: https://claude.ai/artifact/Gg4VL8roc2pc1AnYCoFrRH . One unsourced figure (64.9% People Also Ask) was excluded from the ledger and swapped on the posters that carried it. The four existing artifacts keep their data and interaction engine; their visual layer is rebuilt in the direction Mitch picks. No site/src/public edits, no commits.

Set two (2026-09-22 08:50 EDT, Claude Code): Mitch picked directions 01 (data mandala, blog hero) and 05 (journey map, second graphic; alternate on LinkedIn between personal and the new M² profile), likes 02 as well. Delivered: mandala v2 and journey map v2 under `content-studio/viz/directions/` (readability fixes, 2× posters, GIFs paced three slow renderings + 50 s hold, sectors/territories clickable with postMessage jump to `#intent-<slug>`); expanded post `content-studio/drafts/2026-09-22-search-results-by-intent.blog.md` + `.linkedin.md` (1,982 words, supersedes the 09-14 pair, `approved: no`); research `content-studio/research/2026-09-22/` (zero-click-future ledger, 39 entries; SERP captures 0/10, Google captcha on the Manila IP, script ready for a US IP); M² network targets in the li-syndication repo (`docs/m2-network-targets.md`). Preview: https://claude.ai/artifact/44ZGdGEFqAs5ZsXiVrRXdv . Publish path unchanged (copy `viz/directions/01-data-mandala/index.html` → `public/viz/search-results-by-intent-mandala/index.html`, journey map → `public/viz/search-results-by-intent-journey/`, blogPosts entry, parent-page listener from the draft notes, SERP images to `public/images/blog/serp-examples/`). No site/src/public edits, no commits from the content agents.

Publish wave (2026-09-24 18:55 EDT, Claude Code, records on `main` are authoritative for the release itself): Mitch's blanket "publish now" (2026-09-24). Post 1 `search-results-by-intent` is LIVE on https://mj2.pro/ (gh-pages 7844899 from main 4fcfd6f; RELEASE-READY.md on main carries the mj2.pro procedure; 24/24 study notes restored). LinkedIn post queued for one-tap approval in the li-syndication repo (nothing posted). Wave 2 in progress: post 2 `optimizing-for-ads-in-free-llm-answers` (research `content-studio/research/2026-09-24/llm-ads/`, artifact `content-studio/viz/final/llm-ads-mandala/`, drafts `content-studio/drafts/2026-09-24-ads-in-free-llm-answers.*`), and the three rebuilt artifacts under `content-studio/viz/final/{growth-title-market,b2b-vs-b2c-by-vertical,statistician-vs-data-scientist}/` in the chosen visual language, each with its 09-14 drafts updated to mj2.pro; critics running (`content-studio/routines/runs/2026-09-24-critic-*.md`); one release after they pass, coordinated with the concurrent site-standards deploys (share cards, themes, accessibility, audio for every post; Spanish pilot) from the "Blog writing and social publishing skill" session per `docs/site-standards.md` / `handoffs/2026-09-24-site-standards-build.md`. Mitch's new rule: every post needs audio, a summary_large_image card and a non-headshot og:image; the blog-syndicate gate enforces it on live URLs.

Publish wave 2 LIVE (2026-09-25 00:29 EDT, Claude Code; authoritative release records are on `main`: PROJECT.md "PUBLISHED — September 25, 2026 (00:11 EDT): posts wave 2", handoffs/2026-09-24-posts-wave-2.md, docs/release-2026-09-24-wave2/): https://mj2.pro/blog/optimizing-for-ads-in-free-llm-answers/ , /blog/growth-title-market/ , /blog/b2b-vs-b2c-by-vertical/ , /blog/statistician-vs-data-scientist/ with embeds under /viz/llm-ads-mandala/, /viz/growth-title-market/, /viz/b2b-vs-b2c-by-vertical/, /viz/statistician-vs-data-scientist/ (noindex). gh-pages 94016fa from 3559667; main 6ad50ce; artifact sha256 574ce45b…; sitemap 73; IndexNow 200 (7 URLs); rollback `git revert --no-edit 94016fa` on gh-pages. Critic reports: content-studio/routines/runs/2026-09-24-critic-{titles,b2b,roles,llm-ads}.md. Drafts now `approved: yes`. LinkedIn: routine queues each post for Mitch's one-tap approval (nothing posted). Open for Mitch: sign off the first-person AEO line in post 2; a signed-in check of ZipRecruiter/Glassdoor pay pages (post 3); Search Console sitemap resubmission (73 URLs); the live intent post still says the ads follow-up is "coming" (linking it means re-recording its narration); Semrush keyword research never run; the content-studio infographic sources (viz/directions, viz/final) lack the accessibility and same-origin link fixes applied to the published copies; `main` and `claude/agency-redesign` still need reconciling.

# Mitchell Miller portfolio — current state

Updated September 14, 2026 at 15:13 EDT. Owner: Mitchell Miller. GitHub: https://github.com/CKAWEBUILDER/mitchjmiller.com.git.

## AGENCY REDESIGN — September 14, 2026 (merged: on `main` as `945a3b5`…`bb32728`, released 2026-09-21 as gh-pages `0b9073d`; branch archived 2026-09-25)

Mitch's order (September 14): position the site as an agency (portfolio present but secondary), copy the Contiem.com structure/templates/style with green CTAs instead of teal, add a scrolling brand-logo carousel on the homepage and the Services page, keep SEO.

| Item | State |
|---|---|
| Branch / checkout | `claude/agency-redesign` from `main` `5cf4ab1`, pushed to origin. Its redesign commits reached `main` as `945a3b5`…`bb32728`; the rest was ported on 2026-09-25 and the branch archived (tag `archive/agency-redesign-2026-09-25`, branch left on origin). The checkout `/Users/mitchellmiler/Documents/mitchjmiller-html-migration` is on `main`. Production when this section was written: gh-pages `79de2b6`. |
| Built | Agency shell (`site/layouts/AgencyLayout.astro`, `site/styles/agency.css`, `site/lib/agency.ts`), new home (`site/pages/index.astro`), new `/services/` (manifest kind `added` → 58 published URLs), `site/components/BrandMarquee.astro` fed by `site/data/brands.json` (content worker, 12 entries), all other templates in the new shell with content unchanged; JSON-LD (ProfessionalService + Person on home, Service + FAQPage on services, Article/CreativeWork elsewhere). Copy pack `content-studio/drafts/2026-09-14-agency-copy-pack.md` adopted for lifecycle lines, objectives bodies, services stages/process/FAQ, industries and meta titles. |
| Decisions | Green `#14804a` (4.98:1 white text; 3.13:1 against the navy hero `#0f2440` — the suggested `#0b6b3a` fails 3:1 there); SFC explorer image as the hero photo; five evidence-backed industries; `/work/` stays the complete collection page; standalone SFC report stays outside the shell (parity rule). Full list: [docs/redesign-2026-09-14/README.md](docs/redesign-2026-09-14/README.md). |
| Evidence | typecheck PASS; staging and release builds PASS (verify-parity 57/57 + 5/5 added, 58 sitemap URLs; new verify-agency PASS); JavaScript-off crawl 831/831 over 62 routes; headless Chrome 233/233 with screenshots at 1360/390 under `docs/redesign-2026-09-14/screenshots/`. |
| Open for Mitch | Hero headline (brief's sentence vs. the pack's three options), rights-cleared logo files for the marquee, About opener (§g of the pack) not applied, SFC report inside/outside the shell. |
| Next task | Done: merged into `main` and published on 2026-09-21 (gh-pages `0b9073d` from `main` `bb32728`); later releases are recorded at the top of this file. |

Continuation record: `handoffs/parity-design-2026-09-11.md` ("Agency redesign 2026-09-14"). Status file: `/Users/mitchellmiler/Documents/ai-os/ops/overnight-2026-09-11/status-redesign-build.md`.

## PUBLISHED — September 11, 2026 (release owner: Claude Code)

- 17:14 EDT: gh-pages `1b2d2a6` (previous `923dfd8`) from release source `c422377`; `main` fast-forwarded. Artifact sha256 `18b6093162b76fddff62aa088013eff67090eb2779c4746fd438a00f6ac7f6d7`, rebuilt and hash-matched immediately before deploy.
- Live probes 17:15 EDT after Pages reported `built`: home 200, one h1, no `noindex`; robots allows all and declares the sitemap; sitemap 57 URLs; Search Systems PDF sha256 begins `8f48d7d7` (corrected file); `/blog/gbp-2026-ai-grounding/` and `/blog/studying/hermes-concepts-field-guide/` 200 (formerly 404 shells); `/lab/population-workbench/` and `/clients/` 200; unknown route 404 with the real 404 page; Turnstile on `/contact/`.
- 17:41 EDT follow-up: gh-pages `79de2b6` from source `c52664f` adds only the IndexNow key file `/9b0893b8818bd5bce05d66051f2bc971.txt` (artifact sha256 `6d2a55c4e8e8891c9ad06d941848e1175690b9dd67ba85e42e721512f11c3325`, manifest `docs/release-2026-09-12/artifact-manifest-indexnow.txt`, one-file diff verified). All 57 URLs submitted to IndexNow: `202 Accepted`. Google does not use IndexNow; Search Console submission is a morning item.
- Cloudflare Pages mirror `mitchjmiller-com` carries the 17:14 artifact plus an `X-Robots-Tag: noindex` header scoped to the pages.dev host; custom domains stay pending until the zone exists. Client portal Pages project set to `fail_open: false` at 17:50 EDT.
- Not released: the new design candidate (Codex, private review hub). Production shows the parity appearance.
- Rollback: in a clean gh-pages worktree, `git revert --no-edit 79de2b6 1b2d2a6 && git push origin gh-pages`.
- Morning: Search Console sitemap submission and indexing requests; GA4 realtime check; live contact test (`node cloudflare/api-worker/scripts/leads.mjs 5` from the cloud worktree); design-candidate decision; Cloudflare zone and custom-domain activation.

## Release candidate 2026-09-12 — verified (published as above)

Branch `claude/release-2026-09-12` in worktree `/Users/mitchellmiler/Documents/mitchjmiller-release` (from Codex's `codex/html-parity-design-20260911` at `bc8f019`, plus lane 2 `claude/population-workbench` merged at `a6abd03`). Release source commit `c422377`; artifact hash `18b6093162b76fddff62aa088013eff67090eb2779c4746fd438a00f6ac7f6d7` (sorted sha256 of `dist/`, reproduced by two builds). Public site updated: `gh-pages` is now `79de2b6` (see PUBLISHED above).

| Item | State |
|---|---|
| Content | 53 production-parity routes (exact content, complete HTML) + `/lab/` (three existing tools as islands, workbench card) + `/lab/population-workbench/` and `/methodology/` + `/clients/` sign-in entrance. Sitemap 57 URLs; four Coming Soon placeholders stay reachable/noindex. Design candidate not included. |
| Resume PDFs | Corrected September 10 files at the existing four paths in the release build (`docs/release-2026-09-12/release-files.json`); staging build keeps the July bytes. |
| Contact | Real form → `https://mitchjmiller-api.clearkayakrentalsoahu.workers.dev/contact` with Turnstile; mailto path kept without JavaScript; `?topic=population-simulation` pre-fills. Live submission is a post-publish probe. |
| QA | verify-parity PASS, verify-workbench 58/58, typecheck PASS, JavaScript-off crawl 757/757, headless Chrome 203/203 with screenshots — [qa-report](docs/release-2026-09-12/qa-report.md). |
| Next task | Lead reads the QA report and publishes per [RELEASE-READY.md](RELEASE-READY.md) (clean `gh-pages` worktree from `923dfd8`, replace contents with `dist/`, push, probes; rollback = revert or reset to `923dfd8`), then morning items (Search Console sitemap, GA4 realtime, live contact test, merge to `main`). Decisions for Mitch in the QA report §8. |

Lane status files: `/Users/mitchellmiler/Documents/ai-os/ops/overnight-2026-09-11/`. Continuation record: `handoffs/parity-design-2026-09-11.md` ("Release candidate 2026-09-12").

## Implementation state recorded September 11, 2026 (Codex parity build)

## Completed outcome

The production-parity complete-HTML migration and separate new design candidate are implemented, validated, committed/pushed, and published to the existing owner-private Sites review. The previous authentication interruption is resolved. This task did not change the public domain, DNS or public production deployment.

- [Review hub](https://mitch-portfolio-html-staging.clearkayakrentalsoah.chatgpt.site/review/)
- [Current-site HTML migration](https://mitch-portfolio-html-staging.clearkayakrentalsoah.chatgpt.site/)
- [New homepage candidate](https://mitch-portfolio-html-staging.clearkayakrentalsoah.chatgpt.site/design/)
- [Full SFC case candidate](https://mitch-portfolio-html-staging.clearkayakrentalsoah.chatgpt.site/design/case-studies/sfc-surf-school/)

## Source and deployment

| Item | State |
|---|---|
| Canonical persistent checkout | `/Users/mitchellmiler/Documents/mitchjmiller-html-migration` — Claude's independent release work; preserve its changes |
| Release candidate worktree | `/Users/mitchellmiler/Documents/mitchjmiller-release`, branch `claude/release-2026-09-12` (see the section above) |
| This isolated implementation | `/Users/mitchellmiler/Documents/Career Coach/work/mitchjmiller-parity-20260911` |
| Branch | `codex/html-parity-design-20260911` |
| Verified deployed source | `59d84a980dc6b99302dbecf62f62e399a93f9ea0`; subsequent documentation commits do not imply a different deployment |
| Private review | Sites version2, owner-only; exact IDs/evidence in [publication.json](docs/implementation-2026-09-11/publication.json) |
| Production source baseline | main `2745c7e20cea0a30d0dc3c5341fc609bfde4b3ea` |
| Public deployment baseline | gh-pages `923dfd85597253786da407fe78de6d0011bbfe16`; read live state before any separate release because Claude owns that work |

GitHub source was pushed before the identical commit was mirrored into the existing Sites repository. The validated staging artifact was packaged from this source and privately deployed; terminal status succeeded. No new Site was created. The old private version remains a rollback option. Original user checkout/drafts and Claude proposal/source changes were preserved.

## Implementation and decisions

- 53 published URLs, four retained Coming Soon/noindex placeholders, and404. Full original article/note bodies, native links and production styling. No whole-site client hydration.
- The standalone SFC report remains owner of its original URL with identical body. Four original production PDFs retain exact bytes. Newer review PDFs stay at `/review-assets/files/`.
- New design uses the supplied Contiem direction: navy photographic hero, quiet white header, green actions, enterprise work first, substantial actual work imagery. Both older themes were rejected; they are historical references only.
- `/design/` and its full SFC case share content/image sources with the migration. Applying this candidate to other pages awaits design review.
- Existing three lab demonstrations are bounded islands. No new simulation, CRM or client portal was built by this task.
- Explicit small parity exceptions: native document navigation/enhancements, duplicate identical article listing removed, SFC category repaired for filters, and one historical teaser cleaned of truncated HTML. Full content was not rewritten.
- Staging: noindex and no analytics. Local release mode: exactly53 indexable sitemap URLs, placeholders still noindex, review pages/assets excluded and one analytics loader per eligible page. Build commands do not deploy.

## Validation

[Focused browser QA](docs/implementation-2026-09-11/qa-overnight.md) passed: full SFC candidate desktop/mobile, both menus, resume dialog/Escape, writing/build filters, Mermaid SVG/source fallback, all three lab controls and native resume navigation with JavaScript disabled. Screenshots are linked there. No horizontal overflow at390px on tested views.

[Static staging](docs/implementation-2026-09-11/validation-staging.json), [local release](docs/implementation-2026-09-11/validation-release.json), both TypeScript configurations and [62 HTTP checks](docs/implementation-2026-09-11/validation-http.json) passed. Full checks cover57 retained routes,25 complete article/note bodies,160 case fields,4 exact PDFs, the exact SFC report body, local assets/links/anchors, sitemap/indexing and missing-page404. Local visual parity was checked against an analytics-free export of deployed production. External destination health and public indexing were not claimed.

## Ownership, permissions and next step

Mitch authorized implementation, source pushes, private review publication and coordinated overnight agents. Codex's authorized portfolio work is complete. Review the new homepage/full case, then decide whether to extend the design. Claude is the sole public release owner: the parity track plus lanes 2/3 is the verified release candidate above (release decision recorded in the overnight coordination file); Codex's implementation task did not promote production or alter DNS. Do not infer approval of the rejected themes from older proposals.

The overnight coordinator and Claude use their existing private coordination document and per-owner status files under `/Users/mitchellmiler/Documents/ai-os/ops/overnight-2026-09-11/`. Read their current claims before starting work; do not duplicate Finance, Date Night, DomainSignal, Gmail or AI OS writers.

All task-specific portfolio browser/server resources were closed, viewport/script testing restored, and credentials kept out of source/records. Follow [handoff](handoffs/parity-design-2026-09-11.md) for the concrete next step. [Earlier roadmap](docs/migration-2026-09-11/README.md), [implementation detail](docs/implementation-2026-09-11/README.md), [content evidence](docs/overhaul-2026-09-10/content-evidence.md) remain linked references.
