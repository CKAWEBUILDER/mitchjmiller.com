# Keyword MSV pull — 2026-09-25

`seed-100.txt` is one Semrush Keyword Overview bulk paste (US database, 100 max), ordered in blocks so the export maps back to posts:
- 1–14: primary keywords recorded in `content-studio/drafts/*` (`kw:`), plus the b2b-search-authority article's implied primary (line 14).
- 15–50: recorded secondaries (`kw_secondary:`), minus 13 strings that aren't real queries (Counter-GEO-Bench, claude -p metered billing, and similar).
- 51–80: M² service and money keywords (AEO/GEO/AI SEO, B2B SEO, fractional roles).
- 81–92: the Salesforce + AI-visibility practice post.
- 93: the slogan "measured momentum" (brand-conflict check).
- 94–100: AI search comparison and how-to queries.

Semrush data comes from the web UI only (no API units). Save the export next to this file as `semrush-keyword-overview-2026-09-25.csv` and check it: size above zero, 100 data rows, not a repeat of an earlier download.
