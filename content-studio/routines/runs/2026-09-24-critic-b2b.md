# Critic pass — 2026-09-24 — rebuilt infographic: b2b-vs-b2c-by-vertical (fractal stack)

Critic: independent re-run, read-only except this file. No git, no site build, no Semrush
MCP/API, no purchases/logins/posts. Servers used, all self-started and self-stopped, all ≥5300:
`node export.mjs --verify` (headless Chrome via puppeteer-core, no network server), `node
export.mjs --live` (throwaway 127.0.0.1:5311, script-confirmed "server stopped"), and two
standalone read-only probe scripts I wrote to my scratchpad (127.0.0.1:5340, :5341, both
self-stopped) to test at exactly 1360px and to run adversarial `?parent=` injection attempts.
Opened one Browser-pane tab (`tab-3`) for live re-fetches; closed it at the end. Did not touch
the pre-existing `seed` tab or another concurrent session's tab/local servers on 5302/5303
(evidence this environment is running parallel critic passes today — `2026-09-24-critic-llm-ads.md`
and `2026-09-24-critic-titles.md` both exist alongside this file). Today: 2026-09-24.

Target: `content-studio/viz/final/b2b-vs-b2c-by-vertical/` (index.html, export.mjs, README.md,
3 posters, anim-640x800.gif) — a full rebuild in "the fractal stack" language (direction 02),
replacing the earlier kit-based dashboard critic-C reviewed on 2026-09-14. Drafts
`2026-09-14-b2b-vs-b2c-by-vertical.{blog,linkedin}.md` (blog gained a new "By vertical" section,
2026-09-24). Ledger `research/2026-09-14/b2b-b2c/` (179 rows, unchanged). Prior critic
`routines/runs/2026-09-14-critic-C.md`.

**Methodology note on writes:** `export.mjs --posters` and `--gif` regenerate files inside the
target folder, which I'm not permitted to write to, so I did not run those modes. Instead I
verified the *shipped* `poster-*.png` and `anim-640x800.gif` directly — dimensions/sizes via
`sips`, pixel-level crops via PIL for 1:1 legibility, and the GIF's actual byte structure via a
hand-written GIF89a parser (frame-by-frame, in file order) — which checks the real deliverables
rather than freshly-regenerated ones. `--verify` and `--live` are read-only (audited this in the
source before running) and were run as-is.

**Self-disclosed process error, corrected:** the first PIL crop command was run with the target
folder as its working directory, and 4 `crop_*.png` files were briefly written there alongside
the real deliverables before being copied to my scratchpad. Caught during final cleanup, deleted
immediately (`rm crop_titlebar.png crop_l2_row1.png crop_l3_groupA_B.png crop_l3_gapC_footer.png`),
and the folder now matches its original 7-file listing (same sizes and mtimes as the pre-session
`find` at the start of this pass). No artifact file was overwritten or altered — only 4 stray,
never-referenced-elsewhere PNGs existed transiently. Flagging this against the letter of "write
only the report file," in the same spirit this piece asks of its own sourcing: disclose the gap
rather than quietly fix it and say nothing.

---

## 0. Prior fixes (critic-C, 2026-09-14) — do they still hold?

| Fix | Status | Evidence |
|---|---|---|
| G2 86% via DemandGen 2021, not 92% | **HOLDS** | `index.html` source `g2` cites DemandGen, value 86. Live-refetched the DemandGen URL: exact quote *"86% of buyers turn to peer-review websites to reach a final verdict,"* dated Oct 6, 2021, "2021 B2B Software Behavior Survey." Ledger still carries the old 92%/PR-Newswire row (id order 13) *and* the new 86%/DemandGen row (id order 14) side by side — the artifact correctly uses only the latter. |
| 13× figure labelled B2B-to-B2B | **HOLDS** | On-poster note (verified in the 2160×2700 crop) and `index.html` data note both read: *"Against B2B Education ($1,143) it is a 13× spread — B2B to B2B across verticals, not a B2B-vs-B2C gap."* Blog and LinkedIn drafts state the same. |
| Unbounce/Ruler never asserted as a B2B split | **HOLDS** | Blog: *"neither publisher splits the figure by B2B vs. B2C."* `index.html` source objects for both say the same (Ruler: "not split by B2B/B2C"). Live-refetch of ruleranalytics.com confirms no B2B/B2C breakout exists on the page. |
| Funnel stages never share an axis | **HOLDS** | Architecturally enforced, not just claimed: each `MET` entry carries its own `max`; `l3CellHTML`'s bar-fill percentage is always `value / that metric's own max`, computed per metric id, never combined. Code-read confirmed, plus `export.mjs`'s own audit runs this same invariant live across all 10 verticals with zero failures. |

All four hold, independently re-verified, not just re-asserted.

---

## 1. Checks table

| # | Check | Result | Evidence |
|---|---|---|---|
| 1 | `node export.mjs --verify` (contrast + type-floor + clipping + overlap, both posters, gif canvas, live 1200px×10 verticals, live 390px×10 verticals, keyboard) | **PASS, 0 failures** | `contrast: text 34/34 ≥ 4.5:1 (min 4.81:1) · marks min 3.04:1`; both posters 18 cells/no clipping/no overlap/min 12px; gif canvas no clipping/overlap; live 1200px and 390px each opened and audited all 10 verticals, jump links, `aria-pressed`, live region — 0 fails; Enter-key opens a focused cell. |
| 1 | `node export.mjs --live` (postMessage round trip, target=_top fallback, referrer-ignored, foreign-parent-ignored, no external requests) | **PASS, 0 failures** | Own throwaway server `127.0.0.1:5311`, stopped after (script-confirmed). Listener page received exactly `{"type":"viz-intent","intent":"healthcare"}`; no-listener page landed via `target="_top"` at `#vertical-healthcare`; standalone-with-referrer and standalone-with-foreign-`?parent=` (linkedin.com) both fell back to the mj2.pro URL; zero requests left 127.0.0.1 in any of the four scenarios. |
| 1 | GIF: header, NETSCAPE loop, delay structure, size | **PASS, exact match to README** | Hand-parsed `anim-640x800.gif` in file order (not the histogram — the actual frame sequence): `GIF89a`, 640×800, `NETSCAPE2.0` loop=0 (forever), 248 frames. Delay sequence: frame[0]=150cs (opening still), 9cs×237 (moving frames), 70cs beats at indices [25,53,107,135,189,217] (exactly 2 per rendering × 3 renderings, evenly spaced 82 frames apart — proving three *identical* renderings), 150cs holds at [0,82,164,246] (opening + one per rendering), frame[-1]=4850cs. Total 8003cs = 80.03s. Opening (150cs) + final (4850cs) = 5000cs = 50.0s still at the loop point. File size 2,747,822 B = 2.621 MB, under the 3 MB cap. Every number in the README's motion table and file table checks out exactly, independently re-derived, not just re-read. |
| 2 | Text audit via `export.mjs` (≥12px at 1080, 14 where README claims; no overlap/clipping) | **PASS** | Same run as above. Nothing under 12px anywhere (audit's own fail threshold is <11.95px, never triggered). `.line` and `.cname` (README's "≥14px"/"≥15.75px on square" claims) never appear in either viewport's 12–13.9px breakdown — confirmed they sit at their claimed floor. What *does* sit at 12–13.9px matches the README's own disclosure item-for-item: tags, kickers, `lvl`, `mlabel`, `src`, `val`, `rlabel`, `lhead`, `srcline`/`byline`. |
| 2 | Open PNGs + 2× at 1:1 | **PASS** | Viewed `poster-1080x1350.png` and `poster-1080x1080.png` in full; cropped `poster-2160x2700.png` (the 2×) into 4 native-resolution tiles (titlebar, level-2 row, level-3 groups, footer) via PIL and inspected each at 1:1 — no overlap, no clipping, no rendering artifacts, wires route cleanly, G2 86% (2021) correctly on-poster. Dimensions/sizes confirmed via `sips`: 1080×1080 (293,970 B), 1080×1350 (369,106 B), 2160×2700 (776,538 B) — all match the README's KB figures within rounding. |
| 3 | GIF header/loop/delays/size | **PASS** | See row above — folded in since it's the same evidence. |
| 4 | Interaction at 1360 and 390: clickable + keyboard, level-3 redraw, postMessage exact slugs, standalone links, referrer ignored, foreign `?parent=` ignored, `javascript:` refused, no external requests, reduced motion static | **PASS** | Independent puppeteer probe (own server, port 5340, stopped) at the task's exact widths (not just the builder's 1200px): 1360px zero overflow before/after; clicked `saas-software` → 7 level-3 cells with correct grouping (reviews/cycle=data, lp/site=data, paid/email=no-source, touch=cross-industry); focused `#v-nonprofit`, pressed Enter → selected nonprofit (keyboard works on a *different* vertical than the builder's own test, for independent coverage); zero external requests. 390px: zero overflow, healthcare click opens 7 cells. `?motion=reduce`: headline reads the final "7.3×" on the very first frame and is still "7.3×" 300ms later — never animates. `?parent=javascript:alert(1)` → falls back to the safe default (never becomes the href). All 10 slugs read from `window.VIZ.verticals`, exact string match to the task's required list, in the same order: education, financial-services, real-estate, travel-hospitality, professional-services, saas-software, ecommerce-retail, healthcare, manufacturing-industrial, nonprofit. |
| 4 | Adversarial follow-up: injection/hostname-spoofing on `?parent=` | **PASS (tried to break it, couldn't)** | Second standalone probe (port 5341, stopped): `?parent=https://mj2.pro/"><script>...` → the URL parser percent-encodes the payload into the path before it reaches the page's own escaping helper; no script executed. `data:text/html,<script>...` → rejected, falls back. Hostname tricks all correctly rejected except the one that *should* pass: `mj2.pro.evil.com`, `evil.com/mj2.pro`, `xmj2.pro`, `sub.mj2.pro` all fall back to the safe default; `MJ2.PRO` (case only) is correctly accepted (URL hostnames are case-insensitive by spec — not a hole). |
| 5 | House rules: no hype words, never "agency" | **PASS, none found** | Grepped blog.md, linkedin.md, index.html, README.md for revolutionary/game-changer/groundbreaking/unprecedented/nobody/everyone/"changed everything"/"broke the internet" and for `\bagenc(y|ies)\b` — zero hits in every file. |
| 5 | One keyword per section | **PASS, 1 pre-existing overlap carried forward** | Same overlap critic-C flagged as non-blocking/intentional: "b2b vs b2c customer acquisition cost" is the stated keyword for both TL;DR and "Acquisition cost." The new "By vertical" section uses a distinct keyword, explicitly marked `(editorial, unverified)` — honestly disclosed, ties to the still-open keyword-research gap. |
| 5 | Front-matter complete, `approved: no` | **PASS** | `kw`, `kw_secondary`, `kw_note`, `insight`, `cta`, `title`, `meta_description`, `slug`, `suggested_url`, `date`, `teaser` all present; `approved: no` confirmed. |
| 5 | FAQ + FAQPage JSON-LD valid, matching visible answers | **PASS, 1 cosmetic byte-level diff** | JSON-LD parses (`json.loads`), `@type: FAQPage`, 3 Question/Answer pairs, questions match visible text exactly. Answers match exactly **except** the visible markdown uses "×" (U+00D7) where the JSON-LD uses ASCII "x" in answer 1 ("1.3× / 7×" vs "1.3x / 7x") — confirmed by full-string diff, not just a snippet. Semantically identical, not a factual mismatch; flagged as optional. |
| 5 | Sources section + share CTA | **PASS** | Blog `## Sources`: 7 linked publishers + ledger pointer. `<!--CTA-->` marker present in blog; identical share line closes the LinkedIn post. |
| 5 | Every URL mj2.pro (site's own links); Sources link to real third-party publishers | **PASS** | Zero leftover references to the old `mitchjmiller.com` domain anywhere (grepped all 5 files). Embed `?parent=`, canonical `suggested_url`, jump fallback, LinkedIn link, listener origin check all read mj2.pro consistently. Sources correctly link externally (firstpagesage.com, ruleranalytics.com, unbounce.com, wordstream.com, mailchimp.com, mrbenchmarks.com, demandgenreport.com) — that's the point of a citation, not a violation. |
| 5 | Embed `/viz/b2b-vs-b2c-by-vertical/` with `?parent=` | **PASS** | `src="/viz/b2b-vs-b2c-by-vertical/?parent=https%3A%2F%2Fmj2.pro%2Fblog%2Fb2b-vs-b2c-by-vertical%2F"` — exact path, correctly URL-encoded parent. |
| 5 | `<h3 id="vertical-<slug>">` for all ten, matching artifact slugs | **PASS** | All 10 present in the blog's "By vertical" section, exact string match to `window.VIZ.verticals`, same order. |
| 5 | Byline ~7 min read plausible for ~1,700 words | **PASS** | Recomputed independently (H1 through end of "How to read these numbers," excluding the embed figure/HTML): **1,722 words**, matching the draft's own claim of "1,711" within rounding tolerance. At the draft's stated 230 wpm that's 7.5 min — "~7 min" is a fair rounding, not an overclaim. |
| 5 | LinkedIn under 1,400 chars, required honesty line, share line | **PASS** | Post body (link + hashtags included) = **1,385 chars** exactly, matching the draft's own count, under the 1,400 cap. Contains verbatim: *"The image attached here is the rudimentary version of the living artifact: Education opened, the other nine closed."* Share line present, matches the blog's CTA. |
| 5 | "2026 B2B and 2025 B2C" correct per First Page Sage | **PASS** | Live-refetched both FPS reports: B2B edition published 2026-01-26, B2C edition published 2025-07-03. LinkedIn's "First Page Sage's 2026 B2B and 2025 B2C reports" is correct. |
| 6 | Contrast pairs under 4.5:1 | **PASS, zero** | `export.mjs`'s own pass: 34/34 text pairs ≥ 4.5:1, minimum 4.81:1; non-text marks minimum 3.04:1 (every mark sits beside its printed value, per README). |
| 7 | Publish readiness | See §4 | `approved: no`; `public/viz/b2b-vs-b2c-by-vertical/` does not exist (confirmed — `ls public/viz/` is empty of it); parent-page listener not yet installed in the real page shell; anchors verified against a synthetic test page, not the real rendered post. |

---

## 2. Figure-verification table

Every figure below was re-fetched live from the named primary page during this pass (not just
matched against the ledger). All 59 numeric values in the artifact's `VERTICALS` data structure
were also cross-checked programmatically against `research/2026-09-14/b2b-b2c/data.json` — 59/59
matched on vertical + metric + segment + value, zero mismatches (not shown row-by-row below for
space; spot-checked rows are representative of the full set).

| # | Figure | Ledger row | Live primary-source fetch | Verdict |
|---|---|---|---|---|
| 1 | Education CAC $1,143 (B2B) | `education/B2B/1143`, First Page Sage B2B 2026 | firstpagesage.com/…b2b-edition-fc/: table row "Education → $862 / $1,985 / **$1,143**" | **EXACT** |
| 2 | Education CAC $156 (B2C) | `education/B2C/156`, First Page Sage B2C 2025 | firstpagesage.com/…b2c-edition/: "Higher Education & College" organic $134 / paid $177 (page didn't surface a labelled "combined" cell in this fetch; $156 matches critic-C's prior verified average and FPS's blended methodology confirmed structurally on the B2B table, see note below) | **MATCHES** (see caveat) |
| 3 | Ecommerce CAC $86 (B2B, wholesale) / $66 (B2C) — the 1.3× pair | `ecommerce_retail/B2B/86` + `/B2C/66` | firstpagesage.com B2B table row "eCommerce → $87 / $81 / **$86**," explicitly framed as B2B/wholesale ("Amazon…B2B ecommerce purchases"); B2C page: organic $64 / paid $68 (avg=$66) | **EXACT** |
| 4 | Unbounce — SaaS landing page 3.8% | `saas_software/overall/3.8`, Unbounce 2024 | unbounce.com/conversion-benchmark-report/saas-conversion-rate/: *"The median conversion rate for SaaS landing pages is 3.8%—42% lower than the 6.6% baseline for all industries."* | **EXACT, verbatim** |
| 5 | Ruler Analytics — Software site conversion 7.6% | `saas_software/overall/7.6`, Ruler 2026 | ruleranalytics.com: Software = **7.6%**; page confirmed to organize by 13 industry categories, no B2B/B2C split anywhere on the page | **EXACT, split-claim confirmed absent** |
| 6 | WordStream — Attorneys/Legal CPL $131.63, Travel CPL $44.70 | `professional_services/…/131.63` + `travel_hospitality/…/44.7` | wordstream.com/blog/2026-google-ads-benchmarks (direct browser fetch succeeded — the automated WebFetch tool got a 403 here, matching the ledger's own already-noted 403 pattern for this URL): CPL table "Attorneys and Legal Services → **$131.63**," "Travel → **$44.70**." Also cross-checked all 7 WordStream-sourced figures in the artifact (CTR + CPC + CPL for education, financial services, real estate, travel, professional services, ecommerce, healthcare) — **7/7 exact matches**, live, from the publisher's own page. | **EXACT ×7** |
| 7 | G2 86% (via DemandGen 2021) | `saas_software/B2B/86`, id `s-g2` | demandgenreport.com: *"The research found that 86% of buyers turn to peer-review websites to reach a final verdict."* Published Oct 6, 2021, "2021 B2B Software Behavior Survey." | **EXACT, verbatim** |
| 8 | "By vertical" §, all 10 subsections — every number traces to the artifact | n/a (cross-check) | Extracted 75 distinct numeric tokens from the blog's "By vertical" section; checked each against `index.html`'s raw source. **0 missing** — reproduces the draft's own claim ("75 distinct numbers appear verbatim in index.html") independently. | **CONFIRMED, 75/75** |
| 9 | "By vertical" §, funnel stage + year stated in all 10 subsections | n/a | All 10 name a year (2021/2023/2024/2025/2026) and a funnel-stage descriptor (full funnel / impression→lead / any visit / pre-click research / nurture / first contact→close / cost per donor) for every figure they carry. | **PASS, all 10** |
| 10 | Financial-services CAC $784/$160, 4.9× (spot check of a non-headline pair) | `financial_services/B2B/784` + `/B2C/160` | firstpagesage.com B2B table: "Financial Services → $644 / $1,202 / **$784**." (B2C $160 previously verified by critic-C; not independently re-fetched this pass beyond the org/paid table pull above.) | **EXACT** |

**Note on the FPS "combined" figure:** the B2B page's own table publishes org/paid/**combined**
as three explicit columns — I pulled all 8 B2B rows used by the artifact (Education, Financial
Services, Real Estate, Aviation, Legal, B2B SaaS, eCommerce, Manufacturing) directly from that
table and all 8 match the artifact exactly, including the combined column, which is **not** a
simple average of org/paid (e.g., Education: (862+1985)/2 = 1423.5 ≠ 1143 — FPS uses its own
weighting). This is FPS's own published number, not something the ledger or artifact computed.
The B2C page's fetch in this pass returned org/paid without a clearly-labelled combined cell for
Higher Ed/eCommerce; $156/$66 match what critic-C independently verified on 2026-09-14 against
the same B2C report. Not a new discrepancy — a fetch-coverage note, not a defect.

---

## 3. Ranked fix list

### Blocking

None found. Every content-honesty issue critic-C flagged on 2026-09-14 (13× mislabeling, G2 92%)
still holds fixed; no new factual, sourcing, or mechanical defect surfaced under re-verification,
independent re-fetch of every named primary source, or adversarial testing (injection/hostname
spoofing/`javascript:`/foreign-origin attempts on `?parent=`, all attempted and all safely
handled).

### Optional (real, not urgent)

1. **FAQ JSON-LD vs. visible text: "×" vs "x."** Answer 1 of the FAQPage JSON-LD reads "1.3x…7x" (ASCII) where the visible Markdown reads "1.3×…7×" (U+00D7). Only this one answer, only this one substitution, confirmed by full-string diff — everything else is byte-identical. Cosmetic; an automated schema/visible-text matcher would flag it, a reader would not notice.
2. **`shock-stats.md` still carries the pre-correction phrasing** ("Acquiring a B2C ecommerce customer costs $86," "92% of B2B software buyers") — same pre-existing, disclosed-out-of-scope item critic-C carried forward on 2026-09-14. Still true, still honestly flagged in the blog's own draft notes, still not touching anything published.
3. **One repeated section keyword** ("b2b vs b2c customer acquisition cost" for both TL;DR and "Acquisition cost") — same item critic-C treated as non-blocking/intentional. The new "By vertical" section's keyword is honestly marked `(editorial, unverified)`, tying back to the still-open Semrush/keyword-research gap disclosed in `kw_note`.
4. **Two pre-existing lower-confidence rows remain shown, as before:** financial-services 195-day sales cycle (self-disputed 2×, shown with the disagreement stated) and Unbounce financial-services 8.4% landing-page figure (secondary-aggregator-only). Both already caveated in-artifact (`low: true`, explicit "lower confidence" lines); both still listed in the blog's own "needs Mitch's confirmation" section — unresolved, not hidden.
5. **Anchors verified against a synthetic test page, not the real rendered post.** The site's blog-post renderer injects pre-built HTML into the page (a standard React raw-HTML-render pattern), which is *compatible* with raw `<h3 id>` tags surviving a markdown→HTML step, but whether the actual conversion step preserves them wasn't checked here (would require a site build, out of scope for this critic pass). Same open item the builder's own README already lists under "Not verified."

### Publish gates (unchanged from 2026-09-14, still open)

- `approved: no` — Mitch has not signed off on the piece, the artifact, or the new "By vertical" copy.
- `public/viz/b2b-vs-b2c-by-vertical/` does not exist — confirmed empty; the blog's embed iframe and noscript poster fallback will 404 if published as-is today.
- The parent-page listener (for the smooth-scroll + auto-height behavior) is not yet installed in the real page shell — it's documented, not deployed.
- Keyword research (Semrush UI export or WebSearch) never run, per `kw_note`.

---

## 4. Verdict

This rebuild is a genuine, verified improvement, not a repaint. Every one of critic-C's four
2026-09-14 content-honesty fixes still holds under fresh, independent re-verification — not
re-reading the README's claims, but re-fetching every named primary source live (First Page
Sage B2B and B2C, Unbounce, Ruler, WordStream, DemandGen) and getting exact, often verbatim,
matches on every figure checked, including all 7 WordStream figures and all 8 First Page Sage
B2B rows pulled directly from their tables. The new "By vertical" section's 75 distinct numbers
all trace to the artifact with zero exceptions, and all 10 subsections state a funnel stage and
a year. Mechanically, the rebuild is excellent: `export.mjs --verify` and `--live` both pass
with zero failures; the shipped GIF's actual byte structure (not just its self-report) matches
the README's motion table frame-for-frame, including proof of three genuinely identical
renderings; contrast is 34/34 at a 4.81:1 floor; text never drops below 12px and the README's
"≥14px for captions/names" claim holds exactly where it says it does. I spent real effort trying
to break the interaction layer — script-injection payloads, XSS via `?parent=`, hostname-spoofing
tricks, foreign origins, protocol downgrades, testing at the task's specified 1360px rather than
the builder's own 1200px — and every attempt was safely handled. The only findings are cosmetic
(one "×"/"x" byte mismatch in FAQ schema) or pre-existing and already disclosed (the orphaned
`shock-stats.md` phrasing, one repeated section keyword, two lower-confidence rows still awaiting
Mitch's sign-off, unverified anchor survival on the real site). None of these block publication
on content-honesty or mechanical grounds. What does block publication is unchanged from
2026-09-14 and is a process gate, not a quality gate: Mitch's approval, copying the artifact into
`public/`, and the outstanding keyword pass.

**Verdict: no blocking content or mechanical defects. Ready to publish once the standing
process gates clear — Mitch's approval (`approved: no`), copying `index.html` + the three
posters into `public/viz/b2b-vs-b2c-by-vertical/`, installing the parent-page listener in the
real page shell, and the still-outstanding keyword-research pass. No blocking fixes to the
artifact, drafts, or ledger content itself.**
